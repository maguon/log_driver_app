import React, { Component } from 'react'
import {
    StyleSheet,
    Text,
    View,
    FlatList,
    TouchableOpacity,
    ActivityIndicator,
    InteractionManager
} from 'react-native'


const ApplyDemageImage = props => {
    return (
        <View>
            <Text>ApplyDemageImage</Text>
        </View>
    )
}

export default ApplyDemageImage